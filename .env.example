# Nuxt 本地開發環境連接埠 (npm run dev)
PORT=3000

# ----------------------------------------------------------------------------
# 注意：
# 若要設定 Cloudflare Workers 的 KV 或 Durable Objects 綁定，
# 請前往 `wrangler.toml` 中進行設定。
# `.env` 主要用於純 Nuxt (Node.js) 環境下的環境變數注入。
# ----------------------------------------------------------------------------
